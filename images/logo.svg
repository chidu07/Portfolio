<svg width="400" height="100" viewBox="0 0 300 100" xmlns="http://www.w3.org/2000/svg">
  <!-- Background for contrast -->
  <!-- <rect width="100%" height="100%" fill="black" /> -->

  <!-- Text with unique style -->
  <text x="50%" y="50%" text-anchor="middle" dominant-baseline="middle" font-family="Arial, sans-serif"
        font-size="65" fill="white" font-weight="bold" style="letter-spacing: 2px;">
    Chidananda
  </text>
  
  <!-- Outer glow effect -->
  <!-- <filter id="glow">
    <feGaussianBlur stdDeviation="4" result="coloredBlur"/>
    <feMerge>
      <feMergeNode in="coloredBlur"/>
      <feMergeNode in="SourceGraphic"/>
    </feMerge>
  </filter> -->

  <!-- Applying the glow filter to the text -->
  <text x="50%" y="50%" text-anchor="middle" dominant-baseline="middle" font-family="Arial, sans-serif"
        font-size="65" fill="white" font-weight="bold" style="letter-spacing: 2px;" filter="url(#glow)">
    Chidananda
  </text>
</svg>
